{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "node bin/build.js --dev",
        "build": "node bin/build.js",
        "test": "vitest run",
        "test:watch": "vitest"
    },
    "devDependencies": {
        "esbuild": "^0.25.5",
        "prettier": "^3.5.3",
        "vitest": "^4.1.4"
    }
}
